project.group 'org.dohrm'
project.version '0.0.1'

apply plugin: 'java'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

jar {
    baseName = rootProject.name
    version = version
}

dependencies {

    compile('com.google.guava:guava:18.0')


    testCompile('junit:junit:4.11')
}
